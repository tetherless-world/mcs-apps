#import "./KgNodeFragment.graphql"

query KgNodeSearchResultsPageNodesQuery(
  $kgId: String!
  $limit: Int!
  $offset: Int!
  $query: KgNodeQuery!
  $sorts: [KgNodeSort!]
) {
  kgById(id: $kgId) {
    matchingNodes(
      limit: $limit
      offset: $offset
      query: $query
      sorts: $sorts
    ) {
      ...KgNodeFragment
    }
  }
}
