#import "./KgSourceFragment.graphql"

query KgNodeLabelPageQuery(
  $kgId: String!,
  $nodeLabel: String!
) {
  kgById(id: $kgId) {
    nodeLabel(label: $nodeLabel) {
      context {
        relatedNodeLabels {
          nodeLabel
          nodes {
            id
            pos
          }
          pageRank
          sourceIds
        }
        topEdges {
          object
          predicate
        }
      }
      nodeLabel
      nodes {
        id
        pos
      }
      pageRank
      sourceIds
    }
    sources {
      ... KgSourceFragment
    }
  }
}
