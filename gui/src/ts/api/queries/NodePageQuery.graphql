#import "./NodeFieldsFragment.graphql"

query NodePageQuery(
  $kgId: String!,
  $nodeId: String!
) {
  kg(id: $kgId) {
    nodeById(id: $nodeId) {
      ...NodeFieldsFragment
      subjectOfEdges(limit: 100, offset: 0) {
        object
        objectNode {
          id
          label
          pos
        }
        predicate
      }
    }
  }
}
