#import "./NodeFieldsFragment.graphql"

query NodePageQuery(
  $id: String!
) {
  nodeById(id: $id) {
    ...NodeFieldsFragment
    subjectOfEdges(limit: 100, offset: 0) {
      object
      objectNode {
        id
        label
        pos
      }
      predicate
    }
  }
}
