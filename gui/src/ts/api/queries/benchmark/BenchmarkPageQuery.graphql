query BenchmarkPageQuery($benchmarkId: String!) {
    benchmarkById(id: $benchmarkId) {
        datasets {
            id
            name
        }
        name
        submissions {
            datasetId
            id
            name
        }
    }
}
