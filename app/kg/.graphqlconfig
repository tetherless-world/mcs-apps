{
  "name": "KG GraphQL endpoint",
  "schemaPath": "schema.graphql",
  "extensions": {
    "endpoints": {
      "KG GraphQL Endpoint": {
        "url": "http://localhost:9000/api/graphql",
        "headers": {
          "user-agent": "JS GraphQL"
        },
        "introspect": true
      }
    }
  }
}
